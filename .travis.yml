language: rust
rust:
  - nightly
os:
  - linux
  - osx
  - windows
cache: cargo

before_script:
  - rustup component add rustfmt
script:
  - cargo fmt --all -- --check
  - cargo build --verbose --all
  - cargo test --verbose --all
